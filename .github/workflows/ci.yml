name: CI

on:
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check placeholders exist
        run: |
          grep -q '__MICROCMS_API_KEY__' index.html && echo "OK: API key is a placeholder"